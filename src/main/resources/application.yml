server:
  port: 8081
spring:
  profiles:
    active: prod
  aop:
    auto: true

security:
  basic:
    enable: false
  oauth2:
    resource:
      filter-order: 3

# Mybatis配置
mybatis:
    mapperLocations: classpath:mapper/*.xml
#    configLocation: classpath:mybatis.xml


